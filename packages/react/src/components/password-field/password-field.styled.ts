import styled from '@emotion/styled';

const Root = styled('div')(
  ({ theme }) => `
    /* Your styles */
  `,
);

const XZKUIPasswordFieldStyled = {
  Root,
};

export default XZKUIPasswordFieldStyled;
