import { buttonClasses } from '@mui/base';

const xzkuiButtonClasses = {
  ...buttonClasses,
  loading: 'xzkui-button-loading',
  full: 'xzkui-button-full',
  extraSpaces: 'xzkui-button-extra-spaces',
};

export default xzkuiButtonClasses;
