import { styled, Stack } from '@tamagui/core';
import { TOAST_ANNOUNCE_EXCLUDE_COMPONENT_NAME } from '../toast.constants';

export const ToastAnnounceExcludeFrame = styled(Stack, {
  name: TOAST_ANNOUNCE_EXCLUDE_COMPONENT_NAME,
});
