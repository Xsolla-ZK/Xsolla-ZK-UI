import { Meta, Typeset } from '@storybook/blocks';
import typography from '../tokens/common/typography';

<Meta title="Docs/Typography" />

# Base Typography

**Font:** {typography.font.text}

**Weights Text:** {Object.entries(typography['font-weight'].text).map(([key, value]) => `${value}(${key})`).join(', ')}

**Weights Brand:** {Object.entries(typography['font-weight'].brand).map(([key, value]) => `${value}(${key})`).join(', ')}

<Typeset
  fontSizes={
    Object.values(typography['font-size']).map((item) => parseFloat(item))
  }
  fontWeight={typography['font-weight'].text.accent}
/>